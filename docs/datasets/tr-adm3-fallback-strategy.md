# Turkey ADM3 Fallback Strategy

Every ADM2 district in 973 districts has a default fallback chain. Production resolution uses official -> runtime -> osm -> generated. Experimental sources require explicit opt-in.

Fallback chains are provider-resolution policy, not measured spatial coverage. The build report records source classes as built, not-built, or disabled.
